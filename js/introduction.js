// import module
const person = require('./person');
const cool = require('./myFaces');

console.log(cool.faces)
console.log(cool.face() )
console.log(`${person.firstName} ${person.lastName} is ${person.age} years old`)
// person.sayHello()
